---
description: Audit or improve a personal website or portfolio for SEO and AEO
---

Use the `agentkit-seo-web-portfolio` skill before producing output.

Treat `$ARGUMENTS` as the user's website URL, repository path, page path, or
optimization instruction. For code edits, preserve the existing application
architecture and styling unless the user explicitly asks for visual redesign.
