---
description: Create, validate, index, or use a private VitaeGraph
---

Use the `agentkit-seo-vitaegraph` skill before producing output.

Treat `$ARGUMENTS` as an optional exact VitaeGraph path, source set, validation request, or retrieval task. Use `~/.agentkit-seo/vitaegraph` when no directory is supplied. Do not scan for private data. Inventory supplied material first, finish one relevant domain workflow at a time, keep the compact personal career context file separate, and never publish or overwrite private graph content by default.
